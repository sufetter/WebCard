import React, {useState} from "react";
import {Box, VStack, Flex} from "@chakra-ui/react";
const Sidebar = () => {
  const [w, setW] = useState(72);
  return <Flex bg="#0e1015">Sidebar</Flex>;
};

export default Sidebar;
